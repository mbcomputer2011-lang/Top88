name: anti_lost_keychain
description: Anti-Lost Keychain Flutter App (ESP32-C3) - demo project
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_reactive_ble: ^5.0.3
  audioplayers: ^5.2.1
  flutter_background: ^1.0.0
  # add other packages as needed

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
